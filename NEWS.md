# roben 0.1.3 [2026-05-20]

* Fixed prediction preprocessing so `predict.roben()` reuses the training
  centering values for new observations.
* Fixed `predict.roben()` to return `error = NULL` when `Y.new` is not
  supplied.
* Fixed `print.roben.pred()` so robust prediction errors are labelled as PMAD.
* Updated GitHub links to the current `jrenstat/roben` repository.

# roben 0.1.2 [2025-2-9]

* Maintenance: Updated Maintainer email.





